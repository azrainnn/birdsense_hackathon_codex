# Training-environment dependencies (Google Colab / GPU machine)
# Install with: pip install -r requirements_train.txt
# Do NOT install on Raspberry Pi — see deployment environment in CLAUDE.md

# Data collection
requests>=2.31

# Audio & feature extraction
librosa>=0.10
soundfile>=0.12

# Pretrained bird-call embeddings (BirdNET) — used to extract fixed 1024-d
# feature vectors; we train only a small Dense head on top, no fine-tuning
birdnetlib>=0.18

# Deep learning — classifier head training and TFLite export
tensorflow>=2.21

# Metrics
scikit-learn>=1.4
